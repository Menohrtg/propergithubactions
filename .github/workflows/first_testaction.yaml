name: First workflow
run-name: Deployed by ${{github.actor}}
on:
    push: 
        branches: 
            - main
            - dev
    workflow_dispatch: 
jobs:
    my_first_job:
        name: build job
        runs-on: ubuntu-latest
        steps:
            - name: 🍏checkout step
              uses: actions/checkout@v3
            
