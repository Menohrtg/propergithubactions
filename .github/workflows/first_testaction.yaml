name: First workflow
#run-name: Deployed by ${{github.actor}}
on:
    push: 
        branches: 
            - main
            - dev
    workflow_dispatch: 
jobs:
    my_first_job:
        name: build job
        runs-on: ubuntu-latest
        steps:
            - name: 🍏checkout step
              uses: actions/checkout@v3
            - name: 🍏Powershel
              run: |
                Write-Host "Thisi is going to fail because i want it to fail"
    my_second_job:
        name: second job 
        if: ${{always()}}
        needs: [my_first_job]
        runs-on: ubuntu-latest
        steps: 
            - name: 🍏 checkout step
              uses: actions/checkout@v3
            
